package jsonic.view;

/**
 * Marker interface: each State builds its own concrete implementation, the View
 * uses instanceof to pick the renderer; each implementation carries its own public final fields.
 */
public interface IRenderSnapshot {
}
